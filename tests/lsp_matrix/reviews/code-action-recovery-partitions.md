# Quick Fix S9 recovery partitions

B05.11 closes the four S9 service/protocol Quick Fix cells using the marked
diagnostic recovery fixture. A valid `Array<i64>.frist()` typo remains beside an
unresolved name while edits add incomplete member, call, type and declaration
forms. A later Unicode comment shifts every original range, and restoration
returns the baseline. Each state runs under LF and CRLF.

At every damaged state, service and protocol tests pin the ordered three-fix
set for the valid typo. They check titles, quick-fix kind, the sole document,
one edit at the current marked range, exact replacement, and the protocol's
document version. Live action arrays equal fresh service/server results. The
unresolved name without an import candidate and each incomplete site have no
guessed fix. After the prefix shift, requesting the old byte/UTF-16 range
returns an empty action set.

The first fix is applied to a fresh document in every damaged state. The whole
result must match an independent source oracle, clear only the method typo,
preserve the unresolved name and any type/parse error, and leave no action at
the repaired or incomplete sites. Restoring the original source restores the
current three-fix set. The separate diagnostic S9 cells remain reviewed in
`diagnostic-recovery-partitions.md`.
