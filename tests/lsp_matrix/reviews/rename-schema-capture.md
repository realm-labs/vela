# Schema function lexical capture

B04.16 advances S2/S5/S8 positive/negative service/protocol partitions for
references, highlight, prepare-rename and rename (48 IDs following
`<feature>/syntax/<S2|S5|S8>/<positive|negative>/<service|protocol>`).
Broad cells remain unreviewed.

An independent fixture rejects renaming a schema function into a visible local,
closure capture, later default parameter, module declaration or imported binding.
Its source-backed declaration must also reject a duplicate source declaration.
Qualified calls and retained aliases may use the same spelling as an unrelated
local. Both layers verify exact references, targets and rejected edits, apply safe
edits, compare whole files, regenerate explicit schema metadata, requery and restore
under Unicode LF/CRLF. Protocol checks UTF-16, document versions and ABI annotations.
Reverse capture, schema short-name ambiguity and remaining parameter/variant and
lifecycle partitions remain open.

The fixture has 23 queries across five groups; all four writable groups undergo
safe applied edits. It includes an unused direct import whose proposed name
collides with a module constant. The initial local-capture failure is retained,
and final validation and Windows evidence are recorded in the B04.16 checkpoint.
