# Reference and rename S9 recovery

B04.48 reviews the sixteen S9 positive and negative service/protocol syntax
cells for references, document highlights, prepare rename and rename. The
eleven-action five-file recovery fixture covers malformed preceding and
trailing declarations, an incomplete type position, a member access ending
at a dot and an incomplete call. Repairs separate the damaged states so a
long-lived service/server must both drop obsolete errors and retain current
symbol facts. A fresh service/server independently checks each state.

The valid imported function keeps its exact defining and use sites, local
highlight kinds, prepare range and full multi-file rename edits through every
damaged state. Independent markers define expected sites, including the closed
qualified consumer; a same-named decoy remains separate. The malformed type
position, incomplete member receiver and incomplete call target yield empty
references/highlights and no prepare or rename result. LF/CRLF and Unicode
prefixes exercise byte-to-UTF-16 projection. Service and protocol both check
parse-error presence and clearing; protocol checks published diagnostics and
versioned edits. Candidate diagnostics belong to the diagnostics feature and
are not claimed by these navigation/edit cells.

This closes S9 for these four methods only. Other B04 syntax groups and local
editor interaction gates remain open. B00-B03 snapshots stay fixed, macOS
needs independent current evidence when used, and B16 remains deferred.
