# Schema free-function references and rename

B04.14 advances the S5/S10 positive/negative service/protocol partitions of
references, highlight, prepare-rename and rename (32 requirement IDs following
`<feature>/syntax/<S5|S10>/<positive|negative>/<service|protocol>`).
Broad partitions remain unreviewed.

The independent corpus separates a source-backed schema function, a same-spelled
source function and local callable, and a metadata-only function. Exact reference
sets include calls, function values, closures and closed files. Strings and unknown
qualified paths stay empty. Both layers apply complete edits, regenerate explicit
schema metadata, and requery after edits and restoration under Unicode LF/CRLF.
Schema function parameters, import aliases and lifecycle partitions remain open.

The matrix contains 14 queries and four ownership groups. It exposed missing
schema function references and value-use rename edits. A shared path collector
now supplies references and edits while retaining source/local binding precedence.
Navigation uses the same schema identity, and prepare-rename returns the terminal
name matching its range. Metadata-only functions remain non-renamable. Final
validation and Windows evidence paths are recorded in the B04.14 checkpoint.
