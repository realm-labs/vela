---
title: "Playground"
description: "Vela Playground文档。"
---

import Playground from '../../../components/Playground.astro';

浏览器 Playground 运行一个 WASM 包装后的 `vela_engine`，适合体验语言语法、集合、方法、诊断和返回值。

它有意不暴露真实 Rust host object、文件系统或服务器状态。需要宿主状态写穿的例子会放在 Rust examples 中。
<Playground />
