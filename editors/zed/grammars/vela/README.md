# Vela Grammar Placeholder

The native LSP server provides semantic tokens, diagnostics, formatting, and
all language intelligence. This directory is reserved for a future Tree-sitter
grammar when Vela publishes one for Zed.

Until then, the Zed package remains a thin native-server launcher and language
configuration package.
